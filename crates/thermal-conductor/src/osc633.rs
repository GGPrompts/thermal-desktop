//! OSC 633 shell integration parser.
//!
//! Re-exports all types and the parser from `thermal_terminal::osc633`.
//! The implementation lives in the shared `thermal-terminal` crate.

pub use thermal_terminal::osc633::*;
